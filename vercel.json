{
  "version": 2,
  "builds": [
    {
      "src": "server/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/server/index.ts"
    },
    {
      "src": "/(.*)",
      "dest": "/client/index.html"
    }
  ],
  "installCommand": "npm install",
  "buildCommand": "cd client && npx vite build --outDir ../dist && cd .. && npx esbuild server/index.ts --platform=node --packages=external --bundle --format=esm --outdir=api",
  "outputDirectory": "dist",
  "env": {
    "NODE_ENV": "production"
  }
}
